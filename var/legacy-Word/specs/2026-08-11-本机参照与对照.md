# 本机参照稿 / 旧稿对照（S2 尾巴 · A）

> 侧栏主路径；只读本地；不上云。

## UI

侧栏阶段条下增加「语料条」：
- **选参照**：学口气（`write.referencePath`）
- **对照旧稿**：防重复（`write.comparePath`）
- 展示当前文件名；点条可进撰写设置

## 注入

`buildWritingContext`：
- 参照：节选 ≤1600 字（已有）
- 对照：对本机当前稿 vs 旧稿做段落重合，注入「【旧稿对照】」命中列表（≤8 条、每条截断），提示改写勿照搬

## 验收

1. 侧栏可不进设置完成选参照 / 选对照  
2. 对话上下文含对照命中（有重合时）  
3. 路径只存本机 config  
