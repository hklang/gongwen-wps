# var/ — 参考资料（只读对照）

本目录**不是**运行时依赖，仅供对照旧实现。新功能只改仓根下的 `wps-addin/`、`specs/`、`Code/`。

| 路径 | 来源 |
|------|------|
| `legacy-Word/` | 自 `涛总个人材料/半年工作总结/Word/` 复制（已排除 `node_modules`、`logs`、`.env`、`*.log`） |

内含：`extension/`、`editor/`、`deploy/relay/`、旧 `wps-spike/`、`specs/`、`Code/` 等。

勿在此目录继续开发；密钥未拷入（无 `.env`）。
