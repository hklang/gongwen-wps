# 撰写落稿安全

## 铁律

1. **清空对话**：绝不碰 ActiveDocument  
2. **改正文的每个点击**（整篇 / 光标 / 选定）：必须先 `saveActiveToVersion` 成功，再写入；失败则中止  
3. **禁止**无版本保护的整篇 `Content.Text` 覆盖（用户点「整篇」即视为确认，不再弹对话框）  
4. 整篇写入走 `writeDocumentStyled`（按 `Word/tools/政府公文排版规范.md`：字体/缩进/行距，非 Word 标题样式）

详见 `2026-08-12-WPS撰写正文稿落稿.md`。
