# WPS 加载项：接入旧版「左侧工程面板」（设计方案）

日期：2026-08-11  
状态：**独立窗为主 · 停靠暂放弃（26895+ 挡 Ribbon）**  
工程：`gongwen-wps/wps-addin/`  
依据：`Code/想构思.md` · `Code/写代码.md`  
对照：`var/legacy-Word/editor` `#projAside`  
关联：[`2026-08-11-07-WPS本地素材与模板.md`](./2026-08-11-07-WPS本地素材与模板.md)

---

## 定案

| 项 | 定案 |
|----|------|
| 布局 | **独立窗** `ShowDialog`（`workspace.html` 左右分栏）。停靠 TaskPane 在 26895+ 挡 Ribbon，暂放弃 |
| UI | 一比一旧 `#projAside`（四分区、折叠、右键） |
| 点文件 | 单击选中；**双击 / 右键「用 WPS 打开」** → 只读新标签（已开则切换） |
| 主区 | 正在写的稿仍是主稿；查看材料会多一个只读标签 |

---

## 复刻分层

```text
projpane / projAside.js   展示
projectRoot.js            绑根 / 列目录 / 读删 / 引用
relay + taskpane          引用条 → suggest/chat 带 materials
```

---

## 进度

| 步骤 | 状态 |
|------|------|
| 左窗 UI + 开窗贴左 | ✅ |
| 绑定工程根（FolderPicker）+ 持久化 | ✅ 默认=**当前文档目录**；可手动改绑 |
| 真列表扫 doc/docx（FileSystem） | ✅ |
| 双击/右键只读打开 WPS 新标签 | ✅ |
| 「版本」旁「存版本」→ 当前文档快照到 `版本/` | ✅ |
| 右键引用 → 抽 docx 正文 → 请求带素材 | ✅ |
| 删除工程内文件 | ✅ |
| ☁ 云端模板模态 | ✅ P0：ShowDialog 双轨 + land（见 `2026-08-13-04-WPS云端模板一对一复刻.md`） |
| 素材「转换」 | ⏳ stub |

---

## 文件

| 路径 | 职责 |
|------|------|
| `js/projectRoot.js` | `GwProject` |
| `js/projAside.js` | 左栏 UI |
| `ui/projpane.html` | 左窗页 |
| `js/taskpane.js` | 引用条 + materials |
| `js/ribbon.js` | TaskPane 左右停靠；独立窗备用 |
| `ui/workspace.html` | 单独立窗内左右分栏（备用） |

---

## 怎么试

1. `cd wps-addin && npx wpsjs debug -s`  
2. 打开公文助手 → 左窗点「绑定」选含 `素材/` 的文件夹  
3. 右键 md「引用」→ 右窗出现 `@` 芯片 → 撰写/精修会带上素材正文  
