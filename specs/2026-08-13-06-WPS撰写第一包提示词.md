# WPS 撰写第一包提示词

> 日期：2026-08-13  
> 状态：**进行中**（第一包已落地；钉住为出稿工作面：**改协议中**）  
> 修订：2026-08-13 立项：第一包人看不懂，按 Cursor Agent 重拆  
> 修订：2026-08-13 用户定调：第一包 = 要求 + 钉住原文 + 还有啥；问模型要啥（其余点名取）  
> 修订：2026-08-13 【还有什么】不列 base_draft  
> 修订：2026-08-13 近轮对话进 messages（用户原话），不靠清单「要则 fetch」  
> 修订：2026-08-13 给多份：点过落稿只标记卡片；稿信正文，不进下一轮 messages  
> 修订：2026-08-13 用户确认：对话只传用户原话 + 助手口语；结果稿一律不传  
> 修订：2026-08-13 冒烟：口径过了；JSON 禁止围栏、钉住已附不必 fetch pin；开始改拼包  
> 修订：2026-08-14 改口：未落稿卡片可带进 messages；不让模型另写线索  
> 修订：2026-08-14 摊开整包；Q1～Q5 收束为 Cursor 线程规则  
> 修订：2026-08-14 落地：`pruneHistory` 同钉附最近一次卡片；换钉只留口语；出卡记 pinStart/pinEnd；提示/占位  
> 修订：2026-08-14 **拍板：有钉住则先理解钉住；出稿 md = 钉住整段替换稿，不是全文。@素材是证据不是落稿面。**  
> 修订：2026-08-15 **改口：上轮不附卡片全文；收成【上一轮的情况】（当时原话 + 各案字数）。宿主不按关键词猜加长/缩短；重试不塞井号。**  
> 修订：2026-08-15 **本钉住下每一轮都带【情况】（上上轮也带），不当命令、不附稿；换钉不带旧钉对话。**  
> 修订：2026-08-15 **拼包改为【历史】1. 2. 3. +【本轮要求】+【本轮钉住】；旧轮不进 messages。**  
> 修订：2026-08-15 **撰写/精修同一套历史包；换钉或清钉清空本页对话（落稿跟钉不视为换钉）。**  
> 修订：2026-08-18 **改口：换钉侧栏保留对话与历史方案卡；发给模型仍不带旧钉。清钉（×）仍清空本页对话。落稿跟钉不视为换钉。**  
> 父规格：`2026-08-12-11-WPS对话上下文·要啥给啥.md`  
> 相关：`2026-08-12-01-WPS对话工具环与素材索引.md`、`2026-08-12-05-WPS撰写产出层级.md`  
> 对标：**Cursor Agent**（system 稳定协议；user 几乎只有用户原话；有什么用工具/清单，正文点名取）

---

## 一句话问题

现在发给模型的第一包把「用户要干什么 / 咱们有啥 / 出稿格式 / 取数人设 / 工程目录」揉成一团，人看不懂，模型更容易抓错焦点。

## 对标

| 产品 | 学什么 |
|------|--------|
| **Cursor Agent** | system 短且稳定；本轮 user **几乎就是用户打的那句话**；仓库有什么靠工具/目录，不靠把纪律粘进用户消息 |
| 本产品差异 | 公文落稿要 JSON + 井号（点选层级套字体）；本机素材要沙箱工具 |

不要学：把 20 条「硬性规则」和用户原话糊在同一段【用户问题】里。

---

## 现状（为什么看不懂）

同一条 user 里叠了：

1. 用户原话  
2. 【产出形态】  
3. 【可用上下文清单】  
4. 一段很长的【宿主约束】（JSON + 井号 + 待核实 + 份数 + 禁止声称已写入…）  
5. 中转再包一层【工作区】目录（最多 40 条路径）  
6. 有素材时第一包还换人设成「取数助手、本轮不写终稿」，与用户勾的「给多份」对着干  
7. 无素材时终稿第一包写「禁止调用工具」→ `fetch_context` 调不成，清单成空头支票  

system 另有一套工具纪律 / JSON 纪律，和 user 里的【宿主约束】**重复**。

---

## 目标第一包（人能扫完）

三层，各管一件事：

| 层 | 只放 | 不放 |
|----|------|------|
| **system** | 身份一句；工具怎么用；出稿协议（聊天 / 出结论 / 给多份 + 井号）；一句「缺则点名取，够了再出稿」 | 用户原话、全文、素材正文、取数人设 |
| **user** | **①要求 ②钉住原文（有则附）③还有啥（短清单）** | 纪律散文、【宿主约束】、素材摘录、全文 |
| **tools** | `fetch_context` / `list_files` / `search_materials` / `read_file` | — |

钉住 = 用户亲手划的工作面（对标 Cursor 里 @ 了一段）。有钉住则先理解这一段；出结论/给多份时 `md` 只替换这一段。无钉住则 ② 整块省略。

理想 **user** 长这样（可验收）：

```
【历史1、2】1=最远；2=上一轮。数字越大距离本轮对话越近。
1. 最远：你说「删繁就简」。出了 3 案（约 214字 / 162字 / 140字）。
2. 上一轮：你说「压缩太狠，再留一点」。出了 3 案（约 540字 / 503字 / 465字）。

【本轮要求】
压缩一下

【本轮钉住】
（当前划的原文）

【还有什么】（未附正文；要则点名）
- 全文 doc_full：有，约 6000 字 → fetch_context(["doc_full"])
- 素材：有，约 12 个文件 → list_files / search_materials / read_file
```

理想 **system** 长这样：

```
你是机关公文助手。本轮以【本轮要求】为准。有【本轮钉住】则它是工作面：出稿 md 是这一段的替换稿，不是全文。【历史】只是经过。
全文/素材只对照取据，不进 md。@引用是证据不是落稿面。多份=同一段的不同写法。无钉住才可按要求写更大范围。
【钉住】已附原文则不必再 fetch pin。
【还有什么】里未附的正文，缺则 fetch_context / list_files / read_file，够了再出稿。
出稿：勾了「给多份」则只输出 JSON {reply, options:[{id,md,note}]}，edit=null。禁止 markdown 代码围栏。
本轮点选层级：正文。落稿用井号区分层级。无出处标【待核实】。
```

「问他要啥」= 清单 + 允许 tool，模型用工具就是在要。  
**不是**再立「取数助手、本轮不准写稿」——那会和勾选「给多份」对打。

---

## 选项

### 第一包结构

| | 做法 | 利 | 弊 |
|--|------|----|----|
| **A 推荐（已按用户本轮调整）** | 第一包 user = 要求 + 钉住原文 + 还有啥；system 允许 tool；够了直接出稿 | 工作面一眼可见；其余仍点名 | 钉住很长时包会变大（可截断并标明 truncated） |
| B | 钉住也只报「有 N 字」，等 fetch | 包更短 | 模型可能不取钉住，要求悬空 |
| C | 强制第一轮只能 tool、不能出 JSON | 像「问他要啥」 | **不做**：即取数人设，和给多份打架 |

### 工程目录（「有啥」要细到哪）

| | 做法 | 利 | 弊 |
|--|------|----|----|
| **A 推荐** | 清单只写「素材：有，N 个文件」；目录等 `list_files` | 第一包短 | 多一轮 list |
| B | 第一包附短目录（路径+标题，无摘录） | 选型快，仍是「有啥」不是正文 | 包会变长 |
| C | 继续塞摘录 snippet | — | **不做**（没要就给正文） |

### 近轮对话（按 `想构思` 对标后再裁）

归类：右侧续聊改稿，不是「再塞一个仓库文件」。  
对标：**Cursor Agent 聊天线程**（飞书智能伙伴同构：侧栏对话本身就是上下文）。

Cursor 怎么给：

| | Cursor | 咱们不该发明的 |
|--|--------|----------------|
| 通道 | 就是 `messages` 里一轮轮 user / assistant | 把聊天再做成 `fetch_context(history)` 当文件取 |
| 本轮 user | 用户刚打的那句 + 本轮 @（选区/文件） | 把纪律、清单、历史摘要糊进同一段 |
| 上一轮 user | 当时打的原话（可含当时的 @） | 把【宿主约束】写进历史让模型学舌 |
| 上一轮 assistant | 线程里留回复；**已写入文件的稿以文件为准** | 清单写「近轮对话：3 条，要则 fetch」；让模型另写「给下轮看的备忘」 |

现状（代码已落地）：`pruneHistory` 助手只留口语首句，**不带**各组 md。未点写入时下一轮说「用第二组再改」→ 模型看不见稿，正文里也没有。

**已定（仿 Cursor，只裁差异）**

```
messages:
  system: （协议，稳定）
  user: 近轮用户原话
  assistant: 【情况】你说「…」。出了 N 案（约 …字）。  ← 本钉住下每一轮，不附稿
  user: 【要求】+【钉住】+【还有什么】   ← 仅本轮
```

- 对话**不进**【还有什么】  
- `fetch_context(history)` **不做主通道**  
- **不**让模型另写线索/备忘  
- 侧栏气泡仍短句；带给模型的上一轮是情况叙述，不是命令、不是卡片正文  

**上一轮怎么带（08-15 改口，作废 08-14「同钉附卡片全文」）**

| 块 | 发给模型 |
|--|--|
| 本钉住下每一轮你说的话 | 收进该轮【情况】，不当命令 |
| 各轮卡片 md / note | **不带** |
| 各轮各案体量 | 带「约 N 字」 |
| 换钉后的旧钉对话 | 发给模型不带；侧栏气泡保留 |

工作面仍是本轮【钉住】。没采用就说「在上一稿上改」时，模型只看钉住原文。

Q1～Q5 不再单拍：全文硬顶截断；同轮各组都带；只最近一次出卡片的那轮；历史用户句不附旧钉住；不报已落稿。

**每轮发给模型的整包（人看这张表）**

| 块 | 现在 | 拍板 |
|--|------|------|
| system | 身份、工具、JSON、井号 | **已定** |
| 本轮【要求】 | 你刚打的那句 | **已定** |
| 本轮【钉住】 | 你当前划的原文（有则附） | **已定** |
| 本轮【还有什么】 | 全文/素材：有无+字数，正文不预塞 | **已定** |
| 近轮你说的话 | 气泡原话（不是当时那包【钉住】） | **已定** |
| 近轮模型口语 | 「已出 3 组」这类短句；最近一次收成【上一轮的情况】 | **已定（08-15）** |
| 最近一次卡片 md | **不带**（只报约多少字） | **已定（08-15，作废同钉附全文）** |
| 更早轮的卡片长文 | 不带，只留口语 | **已定** |
| 已点选定的那组 md | 不另报「已落稿」 | **已定** |
| 全文 / 素材正文 | 不预塞，模型点名再取 | **已定** |
| 上一轮读过的文件正文 | 不留在对话里，下轮要再 read | **已定**（不另造记忆） |
| 让模型写「线索/备忘」 | 不做 | **已定** |

卡片：没点写入照常在侧栏；点了选定/光标/整篇 → 标已落稿。侧栏气泡仍短句。  
发给模型的是【上一轮的情况】，不是卡片正文。本轮【钉住】才是工作面。

### 对标收束（08-15）

近轮 = 情况叙述 + 本轮【要求】。上轮原话不当命令。卡片 md 不进 messages。  
发现性：出卡后提示补半句「也可直接说『用第二组再短一点』」；**不加**「带上一轮」勾选/按钮。

### 怕用户不知道能续改（08-14，已拍板）

**A。** 不开关。出卡后提示补半句；有未写入卡片时，输入框占位改成同类话。

### 换到下一段还带上轮稿吗（08-15）

**发给模型不带卡片正文、不带旧钉对话。** 侧栏气泡和历史方案卡换钉后仍在；只有点钉子旁「×」清钉才清空本页对话。工作面是本轮【钉住】。

---

## 本期做 / 不做

**做**

- 拆开 system / user / tools  
- 删 user 里的【宿主约束】【产出形态】长文（并入 system 一两行）  
- 第一包 **要求 + 钉住原文** 必给（无钉住则只有要求）  
- 近轮对话：更早轮原话 + 口语；最近一次收成【上一轮的情况】（原话 + 字数，不附稿）；发现性用提示，不加开关
- 全文 / 素材正文仍点名再给；不塞摘录；第一包【还有什么】**不列底稿**
- 废「取数助手 + 禁止写终稿」
- 废「无素材 → 禁止调用工具」  
- JSON 禁止 markdown 代码围栏；【钉住】已附则不必 fetch pin  
- **有钉住：出稿 md 只替换钉住整段**（system 协议，不是 user 里写焦点说明）  

**不做**

- 猜 intent、在 user 里写【本轮焦点说明】  
- 强制第一轮只取数不出稿  
- 改落稿按钮、井号套字体规则本身（只改它出现在哪一层）；有钉住时「整篇」仍可能用一段盖全文（本次不收）

---

## 已定 / 未完成 / 存疑

| 栏 | 内容 |
|----|------|
| **已定** | 第一包结构照旧；上轮只带【情况】不带卡片正文；**有钉住则 md=钉住整段替换稿不是全文**；宿主不猜加长/缩短；@素材是证据；多份=同一段不同写法；无钉住才可写更大范围；JSON 禁围栏；**换钉侧栏对话保留、发给模型不带旧钉；清钉才清空本页对话；落稿跟钉不视为换钉** |
| **已完成** | 第一包拼包、短 system（08-13）；钉住工作面（08-14）；**上轮情况化 + 去掉关键词/井号重试（08-15）** |
| **未完成** | 重启中转后点验 P11（钉冒段不得出全文稿） |
| **存疑** | 工程目录「有 N 个」vs 短路径列表；有钉住时「整篇」是否应收 |
| **模型建议** | — |

---

## 验收（拍板后才写代码）

| ID | 期望 |
|----|------|
| P1 | 把第一包打印给人看：能在 20 秒内指出「用户要什么」和「有什么」 |
| P2 | user 段不含【宿主约束】【产出形态】 |
| P3 | 有钉住 → 第一包 **含钉住原文**；全文/底稿/素材正文仍不在第一包 |
| P4 | 无论有无素材，第一包都允许 tool |
| P5 | 不再出现「你是取数助手，本轮不写终稿」与「给多份必须输出 options」同包对打 |
| P6 | 第二轮起 messages 含本钉住下各轮【情况】（原话 + 字数，不含稿）；换钉发给模型不带旧钉；侧栏旧气泡仍在；【还有什么】不出现「近轮对话：无」 |
| P7 | 清单与工具说明不再把 history 写成「要则 fetch」 |
| P8 | （作废）「下一轮绝不含稿 md」 |
| P9 | system 写明 JSON 禁止代码围栏；【钉住】已附则不必 fetch pin |
| P10 | 下一轮 assistant history **不含**卡片 md；含【上一轮的情况】+ 字数；侧栏气泡仍短句；出卡提示含「可直接说用第×组再改」 |
| P11 | 钉住冒段出结论/给多份 → 各组 `md` 是该段替换稿，不是全文；仍可 fetch `doc_full` 作对照 |
| P12 | 精修/撰写：划另一段再钉住 → 侧栏原对话还在；活方案卡收成「历史」；再发一轮 → 模型只带本钉情况。点钉子旁「×」→ 本页对话清空。落稿后钉子跟着走 → 对话不清 |

## 落点（拍板后）

| 文件 | 改什么 |
|------|--------|
| `wps-addin/js/taskpane.js` | 出卡片时记下 `pinStart/pinEnd`；出卡提示/占位 |
| `wps-addin/js/contextKernel.js` | `pruneHistory`：最近一次出卡收成【上一轮的情况】，不附 md |
| `wps-addin/js/chatToolLoop.js` | 不要 gather_only 换人设 |
| `var/legacy-Word/deploy/relay/suggest.py` | `_packet_system`：有钉住则 md 替换钉住整段 |

同步 `editor/suggest.py`（本机中转副本）。
